apiVersion: v1
kind: Service
metadata:
  name: {{ include "zitadel-rbac-mapper.fullname" . }}
  labels:
    {{- include "zitadel-rbac-mapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zitadel-rbac-mapper.selectorLabels" . | nindent 4 }}
