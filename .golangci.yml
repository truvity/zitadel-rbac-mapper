version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - misspell
    - unconvert
    - unparam
    - nolintlint
    - gosec
    - gocyclo

  settings:
    gocritic:
      enabled-tags:
        - diagnostic
        - style
        - performance
    misspell:
      locale: US
    gocyclo:
      # Go Report Card uses 15. We use 25 to accommodate complex server
      # setup functions. Anything above 25 should be refactored.
      min-complexity: 25

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unparam
          - gocyclo

formatters:
  enable:
    - gofmt
    - goimports

  settings:
    goimports:
      local-prefixes:
        - github.com/truvity/zitadel-rbac-mapper
